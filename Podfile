# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AppMarvel' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AppMarvel
    pod 'Alamofire', '~> 4.5'
    pod 'Kingfisher', '~> 5.0'
    pod 'SwiftHash'

  target 'AppMarvelTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Alamofire', '~> 4.5'
    pod 'Kingfisher', '~> 5.0'
    pod 'SwiftHash'
  end

end
